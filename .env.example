# ─── Optional environment variables ──────────────────────────
# Restrict which origin can call your Next.js API routes.
# Leave blank to allow all origins (fine for public tools).
ALLOWED_ORIGIN=

# Rate limiting (overrides built-in defaults)
# RATE_LIMIT_MAX=10
# RATE_LIMIT_WINDOW_MS=60000
